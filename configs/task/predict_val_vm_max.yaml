mode: predict
debug: false

load_ckpt: experiments/vm_max/checkpoint_best_cd0.0002_epoch0.pkl

components:
  data: predict_val
  transform: predict
  system: vm
  model: vm

writer:
  __target__: vm
  save_dir: results_val_vm_max
  save_name: denoised
