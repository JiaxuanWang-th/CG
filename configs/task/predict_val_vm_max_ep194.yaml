mode: predict
debug: false

load_ckpt: experiments/vm_max/checkpoint_best_cd0.0002_epoch194.pkl

components:
  data: predict_val
  transform: predict
  system: vm
  model: vm_max

writer:
  __target__: vm
  save_dir: results_val_vm_max_ep194
  save_name: denoised
