# 本地验证集 benchmark：benchmark_val/.../noisy.npy（由 scripts/export_val_benchmark.py 生成）
predict_dataset:
  shuffle: False
  batch_size: 1
  num_workers: 4
  datapath:
    input_dataset_dir: /home/ubuntu/CG/benchmark_val
    use_prob: False
    loader: npy
    data_name: noisy.npy
    ignore_check: false
    data_path:
      shapenet: [
        [./datalist/validate.txt, 1.0],
      ]
