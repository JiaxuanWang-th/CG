# Cloud multires VM: batch 16 (same as train_vm_max_cloud).
data_name: &data_name models/model_normalized.obj
loader: &loader obj

train_dataset:
  shuffle: True
  batch_size: 16
  num_workers: 0
  datapath:
    input_dataset_dir: /home/dataset_train
    use_prob: True
    num_files: 10000
    loader: *loader
    data_name: *data_name
    ignore_check: false
    data_path:
      shapenet: [
        [./datalist/train.txt, 1.0],
      ]

validate_dataset:
  shuffle: False
  batch_size: 1
  num_workers: 0
  datapath:
    input_dataset_dir: /home/dataset_train
    use_prob: False
    loader: *loader
    data_name: *data_name
    ignore_check: false
    data_path:
      shapenet: [
        [./datalist/validate.txt, 1.0],
      ]
